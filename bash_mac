# vim:set ft=sh:

BREWFIX="$(brew --prefix)"
if [ -f $BREWFIX/etc/bash_completion ]; then
    . $BREWFIX/etc/bash_completion;
fi

export VIRTUALENVWRAPPER_PYTHON=/usr/local/bin/python
if [ -f /usr/local/bin/virtualenvwrapper.sh ]; then
    . /usr/local/bin/virtualenvwrapper.sh
fi

if [ -f ~/.aliases_mac ]; then
    . ~/.aliases_mac
fi

export TERM=screen-256color

# homebrew installed vcprompt is from https://bitbucket.org/gward/vcprompt
export VCPROMPT_FORMAT="%b%u%m"

export PATH="$PATH:~/packer"

docker-machine env &>/dev/null && eval $(docker-machine env)

alias vlc="/Applications/VLC.app/Contents/MacOS/VLC"
