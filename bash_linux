#!/bin/bash
# vim:set ft=sh:

export TERM='xterm-256color'

[[ -f ~/.aliases_linux ]] && . ~/.aliases_linux

export VCPROMPT_FORMAT="%b%u%m"

if ! pgrep -u "$USER" ssh-agent > /dev/null; then
    ssh-agent > ~/.ssh-agent-process
fi
if [[ "$SSH_AGENT_PID" == "" ]]; then
    eval "$(<~/.ssh-agent-process)"
fi
